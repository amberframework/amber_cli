name: <%= @name %>

server:
  host: 127.0.0.1
  port: 3000
  secret_key_base: <%= Random::Secure.hex(32) %>

database:
<% case @database
when "mysql" -%>
  url: mysql://localhost:3306/<%= database_name %>_test
<% when "sqlite" -%>
  url: sqlite3:./db/<%= database_name %>_test.db
<% else -%>
  url: postgres://localhost:5432/<%= database_name %>_test
<% end -%>

session:
  key: <%= @name %>.session
  store: signed_cookie
  adapter: memory
  expires: 0

logging:
  severity: warn
  colorize: true
